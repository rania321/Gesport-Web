<form method="post" action="{{ path('app_inscritournoi_delete', {'idit': inscritournoi.idit}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inscritournoi.idit) }}">
    <button class="btn">Delete</button>
</form>
