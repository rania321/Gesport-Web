{% extends 'base.html.twig' %}

{% block title %}Inscritournoi{% endblock %}

{% block body %}
    <h1>Inscritournoi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idit</th>
                <td>{{ inscritournoi.idit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_inscritournoi_index') }}">back to list</a>

    <a href="{{ path('app_inscritournoi_edit', {'idit': inscritournoi.idit}) }}">edit</a>

    {{ include('inscritournoi/_delete_form.html.twig') }}
{% endblock %}
