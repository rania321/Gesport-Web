<form method="post" action="{{ path('app_reservationactivite_delete', {'idr': reservationactivite.idr}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservationactivite.idr) }}">
    <button class="btn btn-primary rounded-pill py-2 px-4">supprimer</button>
</form>
