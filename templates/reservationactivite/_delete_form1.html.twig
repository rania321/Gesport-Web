<form method="post" action="{{ path('app_reservationactivite_deleteBack', {'idr': reservationactivite.idr}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservationactivite.idr) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>