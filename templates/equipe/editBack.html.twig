{% extends 'baseBack.html.twig' %}

{% block title %}Edit Equipe{% endblock %}

{% block content %}
<div class="container">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div style="height: 30px;"></div> 
            <div class="card">
                
                <div class="card-body">
    <h1 class="card-title">Edit Equipe</h1>

    {{ include('equipe/_form.html.twig', {'button_label': 'Update', 'button_class': 'btn-inverse-warning'}) }}
    <div class="d-flex justify-content-between align-items-center mt-3">
        <div>
            {{ include('equipe/_delete_form.html.twig') }}
        </div>
        <div>
    <a href="{{ path('app_equipe_indexBack') }}" class="btn btn-inverse-warning">back to list</a>
</div>
</div>
</div>
</div>
</div>
</div>
</div>
   
{% endblock %}
