<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Star Admin2</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <style>
        .login-container {
            padding: 50px;
            background-image: url('{{ asset('FrontOffice/img/backlogin.jpg') }}'); /* Chemin vers l'image */
            background-size: cover; /* Redimensionne l'image pour remplir l'élément entièrement */
            background-position: center; /* Centre l'image horizontalement et verticalement */
            background-repeat: no-repeat; /* Ne répète pas l'image */
        }

        .login-form {
            max-width: 400px;
            margin: auto;
            background-color: #fff; /* Couleur de fond du formulaire */
            padding: 30px;
            border-radius: 10px; /* Coins arrondis */
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1); /* Ombre */
        }

        .login-form input[type="email"],
        .login-form input[type="password"] {
            border-radius: 5px; /* Coins arrondis des champs de saisie */
            margin-bottom: 20px;
        }

        .login-form button {
            border-radius: 5px; /* Coins arrondis du bouton */
            background-color: #007bff; /* Couleur de fond du bouton */
            border: none;
        }

        .login-form button:hover {
            background-color: #0056b3; /* Couleur de fond du bouton au survol */
        }

        .login-form p {
            margin-top: 20px;
        }

        .login-container h3 {
            color: #fff; /* Couleur du texte */
        }
    </style>
</head>

<body>
    <section class="vh-100 d-flex align-items-center">
        <div class="container">
            <div class="row">
                <div class="col-md-6 mx-auto login-container">
                    <h3 class="text-center mb-4">LOG IN</h3>
                    <form class="login-form" action="/user/login" method="POST">
                        
                        <div class="text-center mb-5">
                        <img src="{{ asset('FrontOffice/img/logo1.png') }}" alt="Logo" class="img-fluid" style="max-height: 60px;">
                    </div>
                        <div class="form-outline mb-4">
                            <label class="form-label" for="form2Example18">Email address</label>
                            <input type="email" name="email" id="form2Example18" class="form-control form-control-lg" />
                        </div>
                        <div class="form-outline mb-4">
                            <label class="form-label" for="form2Example28">Password</label>
                            <input type="password" name="password" id="form2Example28" class="form-control form-control-lg" />
                        </div>
                        <div class="pt-1 mb-4">
                            <button class="btn btn-primary btn-lg btn-block" type="submit">Login</button>
                        </div>
                        <p class="small mb-3"><a href="{{path('forgotten_password')}}">Forgot password?</a></p>
                        <p class="mb-0">Don't have an account? <a href="{{path('app_register')}}">Register here</a></p>
                    </form>
                </div>
            </div>
        </div>
    </section>
</body>

</html>
