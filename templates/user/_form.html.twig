{#{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }#}
{{ form_start(form, {'attr': {'class': 'form-group'}}) }}

    {# Champ Nom #}
    <div class="form-group row">
        <label for="nomu" class="col-sm-2 col-form-label">Nom</label>
        <div class="col-sm-10">
            {{ form_widget(form.nomu, {'attr': {'class': 'form-control', 'placeholder': 'Entrez votre nom'}}) }}
        </div>
    </div>

    {# Champ Prénom #}
    <div class="form-group row">
        <label for="prenomu" class="col-sm-2 col-form-label">Prénom</label>
        <div class="col-sm-10">
            {{ form_widget(form.prenomu, {'attr': {'class': 'form-control', 'placeholder': 'Entrez votre prénom'}}) }}
        </div>
    </div>

    {# Champ Email #}
    <div class="form-group row">
        <label for="emailu" class="col-sm-2 col-form-label">Email</label>
        <div class="col-sm-10">
            {{ form_widget(form.emailu, {'attr': {'class': 'form-control', 'placeholder': 'Entrez votre email'}}) }}
        </div>
    </div>

    {# Champ Mot de passe #}
    <div class="form-group row">
        <label for="mdpu" class="col-sm-2 col-form-label">Mot de passe</label>
        <div class="col-sm-10">
            {{ form_widget(form.mdpu, {'attr': {'class': 'form-control', 'placeholder': 'Entrez votre mot de passe'}}) }}
        </div>
    </div>

    {# Champ Rôle #}
    <div class="form-group row">
        <label for="roleu" class="col-sm-2 col-form-label">Rôle</label>
        <div class="col-sm-10">
            {{ form_widget(form.roleu, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    {# Bouton Enregistrer #}
    <div class="form-group row">
        <div class="col-sm-10 offset-sm-2">
            <button type="submit" class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
        </div>
    </div>

{{ form_end(form) }}
